# Runtime dependencies loaded indirectly by analysis packages during R Markdown rendering.
# This file is intentionally not sourced; it lets renv record packages that are required at runtime but not called directly in the analysis scripts.
if (FALSE) {
  library(assertthat)
  library(marginaleffects)
}
